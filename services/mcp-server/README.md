# GaaS Model Context Protocol (MCP) Server

This microservice implements the Model Context Protocol (MCP) interface for the AI Gateway.

## Responsibilities
- Expose routing, model, and budget state to client coding assistants (Cursor, Claude Code)
- Provide schema-validated tools for query path selection
- Handle token checks and key validations
